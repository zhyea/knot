package org.chobit.knot.gateway.adapter.request;

public record RequestAdapterDefinition(String code,
                                       String label,
                                       String className) {
}
