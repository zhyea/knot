<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.chobit.knot.gateway.mapper.RoutingRuleMapper">

    <select id="list" resultType="org.chobit.knot.gateway.entity.RoutingRuleEntity">
        select id, name, strategy_type, priority, condition_expr, target_provider_id, target_model_id, status
        from routing_rules
        order by priority asc, id desc
    </select>

    <select id="getById" resultType="org.chobit.knot.gateway.entity.RoutingRuleEntity">
        select id, name, strategy_type, priority, condition_expr, target_provider_id, target_model_id, status
        from routing_rules
        where id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into routing_rules(name, strategy_type, priority, condition_expr, target_provider_id, target_model_id, status)
        values(#{name}, #{strategyType}, #{priority}, #{conditionExpr}, #{targetProviderId}, #{targetModelId}, #{status})
    </insert>

    <update id="update">
        update routing_rules
        set name = #{name},
            strategy_type = #{strategyType},
            priority = #{priority},
            condition_expr = #{conditionExpr},
            target_provider_id = #{targetProviderId},
            target_model_id = #{targetModelId},
            status = #{status}
        where id = #{id}
    </update>

    <select id="listHitLogs" resultType="org.chobit.knot.gateway.entity.RoutingHitLogEntity">
        select id, rule_id, from_target, to_target, reason, hit_time
        from routing_hit_logs
        order by id desc
    </select>

</mapper>
