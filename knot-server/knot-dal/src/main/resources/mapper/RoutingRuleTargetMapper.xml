<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.chobit.knot.gateway.mapper.RoutingRuleTargetMapper">

    <resultMap id="RoutingRuleTargetResultMap" type="org.chobit.knot.gateway.entity.RoutingRuleTargetEntity">
        <id property="id" column="id"/>
        <result property="ruleId" column="rule_id"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="priority" column="priority"/>
        <result property="primary" column="is_primary"/>
        <result property="targetCode" column="target_code"/>
        <result property="targetName" column="target_name"/>
        <result property="modelType" column="model_type"/>
        <result property="providerId" column="provider_id"/>
    </resultMap>

    <sql id="RuleTargetSelectColumns">
        rt.id,
        rt.rule_id,
        rt.target_type,
        rt.target_id,
        rt.priority,
        rt.is_primary,
        coalesce(m.model_code, mp.pool_code) as target_code,
        coalesce(m.name, mp.name) as target_name,
        coalesce(m.model_type, mp.model_type) as model_type,
        m.provider_id
    </sql>

    <select id="listByRuleIds" resultMap="RoutingRuleTargetResultMap">
        select <include refid="RuleTargetSelectColumns"/>
        from routing_rule_targets rt
        left join models m on rt.target_type = 'MODEL' and rt.target_id = m.id
        left join model_pools mp on rt.target_type = 'MODEL_POOL' and rt.target_id = mp.id
        where rt.rule_id in
        <foreach collection="ruleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        order by rt.rule_id asc, rt.priority asc, rt.id asc
    </select>

    <select id="listByRuleId" resultMap="RoutingRuleTargetResultMap">
        select <include refid="RuleTargetSelectColumns"/>
        from routing_rule_targets rt
        left join models m on rt.target_type = 'MODEL' and rt.target_id = m.id
        left join model_pools mp on rt.target_type = 'MODEL_POOL' and rt.target_id = mp.id
        where rt.rule_id = #{ruleId}
        order by rt.priority asc, rt.id asc
    </select>

    <delete id="deleteByRuleId">
        delete from routing_rule_targets where rule_id = #{ruleId}
    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into routing_rule_targets(rule_id, target_type, target_id, priority, is_primary)
        values(#{ruleId}, #{targetType}, #{targetId}, #{priority}, #{primary})
    </insert>

</mapper>
