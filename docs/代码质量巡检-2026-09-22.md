# 全局代码质量巡检 — 2026-09-22

> **本次为只读巡检，未修改任何生产代码。** 所有结论来自本机真实扫描（javac `-Xlint:all` 编译 +
> Python 静态扫描）。扫描脚本已保留在 `.workbuddy/audit/`，可随时复跑复核（见第 8 节）。

---

## 1. 规模基线

| 维度 | 数值 |
|---|---|
| 后端 Java（main） | **414 个文件 / 23,130 行**，7 个 Maven 模块 |
| 后端 Java（test） | 7 个文件 |
| MyBatis | 33 个 mapper XML ↔ 33 个 Mapper 接口 |
| 前端 | 111 个 `.vue` + 51 个 `.js` / **20,098 行** |
| 前端样式 | 2 个 CSS / 513 行 |
| 路由 | 45 条 |
| 数据库 | `schema.sql` 52 张表（ks_/kb_/kr_/kx_ 前缀） |

最大文件（后端）：`RoutingRuleService.java` 1049 行、`BillingService.java` 543 行。

## 2. 编译告警（`-Xlint:all`）

`mvn -B clean compile -DskipTests -Dmaven.compiler.compilerArgument=-Xlint:all` → **BUILD SUCCESS**

| 类别 | 数量 | 说明 |
|---|---|---|
| MapStruct `Unmapped target property` | 4 | 见下表，**均为有意设计** |
| JDK25 lombok `sun.misc.Unsafe` 噪声 | 5 | 不可修，未来 JDK 升级预警 |

| 文件:行 | 未映射属性 | 判定 |
|---|---|---|
| `UserConverter.java:14` | `password` | ✅ 有意：密码不向外映射 |
| `BillingConverter.java:19` | `deleted` | ✅ 有意：逻辑删除标记 |
| `DepartmentConverter.java:18` | `isDeleted` | ✅ 有意：同上 |
| `SystemConverter.java:16` | `moduleCode, actionCode, targetId, resultStatus` | ✅ 有意：审计字段由切面填 |

**javac 本身的告警为 0。**

## 3. 后端静态扫描

| 检查项 | 结果 | 可信度验证 |
|---|---|---|
| 未使用私有方法 | **0** | 扫描器输入 = 399 个 private 方法（非零，非脚本 bug） |
| 未使用私有字段 | 45 | ⚠️ **全部为误报** — 均为 `*Entity.java` 字段，由 MyBatis resultMap 反射填充 / VO JSON 序列化消费，源码内无直接引用属正常 |
| 空 catch（真静默） | **0** | 扫描器输入 = 57 个 catch（非零） |
| 空 catch（命名 `ignored` 的有意忽略） | 0 | — |
| 超长方法（>60 行） | 1 | `RoutingRuleService.java:246 testInvoke` 68 行 |
| `TODO` / `FIXME` / `HACK` / `XXX` | **0** | — |
| `printStackTrace` / `System.out` / `System.err` | **0** | — |
| `@Transactional` 用在 private 方法（Spring 不生效） | **0** | 全部 85 处均在 public 方法上 |
| MyBatis `${}` 字符串拼接（注入风险） | **0** | 33 个 mapper 全量扫 |

> 关于「未使用私有字段 45」：逐条核对后确认全部落在 `knot-dal/.../entity/` 下的实体类
> （`createdAt`/`updatedAt`/`appName`/`roleNames` 等联表冗余字段）。报出来只为留痕，**不建议删**。
> 唯一非实体类的两条是 `GatewayRequestContext.java:20-21` 的 `authorization` / `apiKey`，
> 属上下文承载字段，同样保留。

## 4. MyBatis 与数据库一致性

| 检查项 | 结果 |
|---|---|
| XML 有 statement 但接口无方法（死 SQL） | **0** |
| 接口有方法但无 statement / 无注解 SQL | **0**（1 条 `Param()` 为注解残留误报） |
| XML 无对应接口的孤儿文件 | 0 |
| mapper 中残留旧表名（52 张表改名后） | **0** |
| schema 定义但代码从未读写的表 | **2** ← 见下 |

### ⚠️ 两张「建了但没接线」的表

| 表 | schema | data.sql 种子 | mapper 读写 | 判定 |
|---|---|---|---|---|
| `kb_plugin_config_versions` | ✅ 有 | ✅ 有（data.sql:524） | ❌ 无 | 插件配置版本功能未落地 |
| `kr_plugin_execution_logs` | ✅ 有（schema.sql:757） | ❌ 无 | ❌ 无 | 插件执行流水完全未接线，且是唯一的 `kr_` 前缀表 |

> 注：XML 中「引用但未定义」的 `setting_value` / `status` 为扫描器把 `set status = ...` 误当表名，已排除。

## 5. 前端静态扫描

| 检查项 | 结果 |
|---|---|
| `console.*` 残留 | **0** |
| `debugger` | **0** |
| Vue SFC 中 import 后未使用 | **0** |
| i18n key 缺失 | **0**（定义 124 / 引用 37） |
| 路由重复完整路径 | **0**（45 条；`policy`/`rules` 为不同父路由下的子路径，合法） |
| 路由懒加载目标不存在 | **0** |
| 孤立文件（零引用） | **6** ← 见下 |
| CSS 孤儿类 | **4** ← 见下 |
| 硬编码 `http://` | 1 处（有 env 兜底，见下） |

### ⚠️ 6 个孤立前端文件（合计 343 行）

| 文件 | 行数 | 说明 |
|---|---|---|
| `views/system/RoleManageView.vue` | 70 | 角色管理页未挂路由 |
| `components/system/RoleListPanel.vue` | 33 | 同上，配套列表面板 |
| `components/common/ThemeFloatingPicker.vue` | 123 | 悬浮主题选择器，未接线 |
| `components/business/HealthStatusCard.vue` | 48 | 健康检查卡片，未接线 |
| `composables/useHealthStatus.js` | 28 | 配套 composable |
| `components/common/JsonCodeDialog.vue` | 41 | JSON 展示弹窗，未接线 |

（`main.js` 为入口文件，不算孤儿。）

### CSS 孤儿类 4 条

`.theme-switcher`、`.list-page-shell--fill`、`.list-page-block--fill`、`.list-page-toolbar--end`

> 另有 10 条 `.theme-dot--{blue,gold,green,mist,orange,purple,red,slate,teal}` 被反查判为零引用，
> 实为 `ThemeFloatingPicker.vue:10,23` 的 `'theme-dot--' + theme.key` **动态拼名**，属误报，不计。

### 硬编码地址 1 处

`components/routing/RoutingRuleTestDrawer.vue:166`
```js
const GATEWAY_BASE_URL = import.meta.env.VITE_GATEWAY_BASE_URL || "http://127.0.0.1:9090";
```
有环境变量兜底，仅 fallback 写死 localhost → **P3 nit**，非缺陷。

## 6. 已确认干净（逐项验证过，非"扫不出来"）

- 后端 `TODO/FIXME/HACK`、`printStackTrace`、`System.out/err` — 全 0
- 空 catch（含真静默档）— 0，且已用第二种写法复验（57 个 catch 输入）
- 未使用私有方法 — 0，输入 399 个方法已验证非零
- MyBatis `${}` 注入风险 — 0
- 表改名后旧表名残留 — 0（33 个 mapper 全量扫）
- 前端 `console.*` / `debugger` / 未使用 import / i18n 缺失 / 路由断链 — 全 0
- `@Transactional` 误用在 private 方法 — 0

## 7. 优先级与建议动作

| 级别 | 项 | 动作 | 代价 |
|---|---|---|---|
| **P2-1** | 6 个孤立前端文件（343 行） | **删**或**接线**。角色管理页 + 列表面板建议接线（功能明显缺失）；主题选择器/健康检查/JSON 弹窗确认不需要则删 | 低 |
| **P2-2** | `kr_plugin_execution_logs` 空表 | 决策：插件执行流水功能是否要做。不做则删表；做则补 mapper + 写入点 | 中 |
| **P2-3** | `kb_plugin_config_versions` 有 seed 无读写 | 同上，且种子数据已在库里空转 | 中 |
| **P2-4** | `RoutingRuleService.java` 1049 行 / `testInvoke` 68 行 | 按职责拆分（`testInvoke` 可独立成 `RoutingRuleTestService`） | 中 |
| P3-1 | 4 条 CSS 孤儿类 | 删除 | 低 |
| P3-2 | 4 条 MapStruct `Unmapped` 告警 | 加 `@Mapping(target="password", ignore=true)` 显式化，让告警归零且意图可见 | 低 |
| P3-3 | `http://127.0.0.1:9090` fallback | 改为从运行时配置取，或保留但加注释 | 低 |
| P3-4 | `BillingService.java` 543 行 | 观察，暂不动 | — |

## 8. 复现命令

```bash
# 编译告警
mvn -B -f knot-server/pom.xml clean compile -DskipTests -Dmaven.compiler.compilerArgument=-Xlint:all

# 后端死代码 / 空 catch / 超长方法
python .workbuddy/audit/audit_knot.py "D:/MyDevelop/JDevelop/workspace/knot/knot-server"

# 前端孤立模块 / i18n / 路由
python .workbuddy/audit/audit_front.py

# MyBatis 一致性 + schema 覆盖
python .workbuddy/audit/audit_mybatis.py

# CSS 反向核对
python .workbuddy/audit/css_check.py

# 扫描器自检（确认「报 0」不是脚本 bug：输出 private 方法数 / catch 数必须非零）
python .workbuddy/audit/verify.py
```

## 9. 门禁状态

- `mvn clean compile`（7 模块全量）→ **BUILD SUCCESS**，门禁未破
- 前端 `vite build` → 上一轮 EXIT=0（本次未重跑，因巡检未改前端代码）
- `mvn test` 本次未跑（巡检为只读，未改动源码，无需回归）
