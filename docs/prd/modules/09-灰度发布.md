# 灰度发布模块产品文档（可选）

## 1. 模块目标
- 降低版本变更风险，支持按比例逐步放量并可快速回滚。

## 2. 功能范围
- 灰度对象：网关功能、模型版本、路由规则、频控配置。
- 灰度策略：按应用、用户、流量比例、时间窗口。
- 风险控制：自动回滚阈值、人工确认发布。

## 3. 关键接口（建议）
- `POST /api/release/gray-plans`
- `POST /api/release/gray-plans/{id}/publish`
- `POST /api/release/gray-plans/{id}/rollback`
